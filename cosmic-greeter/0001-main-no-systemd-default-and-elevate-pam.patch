--- a/Cargo.toml
+++ b/Cargo.toml
@@ -31,7 +31,7 @@
 tracing.workspace = true
 tracing-journald = { workspace = true, optional = true }
 tracing-subscriber = { workspace = true, features = ["env-filter"] }
-pam-client = "0.5.0"
+elevate-pam = { git = "https://gitlab.com/zainium-dynamics/elevate-privilege.git", rev = "c7604ee", default-features = false, features = ["std", "dynload", "elevate_crypto"] }
 pwd.workspace = true
 ron.workspace = true
 shlex = "1.3.0"
@@ -91,8 +91,10 @@
 features = ["tokio-codec"]
 
 [features]
-default = ["logind", "networkmanager", "upower", "systemd"]
-logind = ["logind-zbus", "systemd"]
+# Zainium: quantra-logind on system bus — no systemd / journald required.
+# networkmanager optional (Zainium uses quantra-net; enable if NM is present).
+default = ["logind", "upower"]
+logind = ["logind-zbus"]
 systemd = ["tracing-journald"]
 networkmanager = ["cosmic-dbus-networkmanager"]
 upower = ["upower_dbus"]
