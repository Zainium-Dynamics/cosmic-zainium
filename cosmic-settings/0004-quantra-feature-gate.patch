--- a/cosmic-settings/src/main.rs
+++ b/cosmic-settings/src/main.rs
@@ -7,9 +7,9 @@
 #![allow(clippy::too_many_lines)]
 
-#[cfg(not(any(feature = "systemd", feature = "openrc")))]
+#[cfg(not(any(feature = "systemd", feature = "openrc", feature = "quantra")))]
 compile_error!(
     "At least one service manager feature must be enabled. \
-     Enable 'systemd' or 'openrc' for bluetooth service management."
+     Enable 'systemd', 'openrc', or 'quantra' for bluetooth service management."
 );
