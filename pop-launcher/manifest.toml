# manifest.toml — pop-launcher (syshub tier)

[package]
name             = "pop-launcher"
version          = "1.5.0"
description      = "pop-launcher search backend used by cosmic-launcher, systemd/dbus/flatpak free"
license          = "GPL-3.0-only"
maintainer       = "Ali Zain <alizain@zainiumdynamics.tech>"
homepage         = "https://github.com/pop-os/launcher"
build_type       = "dynamic"
libc_target      = "musl"

blake3           = ""
ed25519_sig      = ""
ed25519_pubkey   = ""

depends          = []
provides         = ["pop-launcher"]
tags             = ["desktop", "cosmic"]

[install]
bin     = "/overlayer/syshub/bin"
_syshub = true

[remove]
files    = []
dirs     = []
symlinks = []

[hooks]
